This directory contains all the source code and the input file.

example.py contains few example function calls. 

sol_remover.py contains all the actual source code and input.dat all
parameters needed to run the script. The easiast way to use the script is 
to just edit the file input.dat and then to run: "python3 sol_remover.py".
